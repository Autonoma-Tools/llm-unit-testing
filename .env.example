# Copy to .env and fill in. .env is gitignored. Never commit a real key.
#
# Only the live tiers need this. The every-commit tier (tests/test_prompt_logic_mocked.py,
# tests/test_structured_output.py, tests/test_string_assertions.py,
# tests/test_semantic_similarity.py, tests/test_llm_judge.py) runs with this file absent.
OPENAI_API_KEY=sk-replace-me
